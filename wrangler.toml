name = "kaffee-faensen"
compatibility_date = "2025-01-15"
pages_build_output_dir = "."
compatibility_flags = ["nodejs_compat"]

# Environment Variables - Plain text variables can be set here
# Secrets (encrypted) must be set via Cloudflare Dashboard or `wrangler secret put`
[vars]
SITE_URL = "https://kaffee-faensen.pages.dev"
