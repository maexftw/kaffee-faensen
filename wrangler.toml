name = "kaffee-faensen"
compatibility_date = "2025-01-15"
pages_build_output_dir = "."

# Optional defaults – override via Cloudflare Dashboard or `wrangler secret`
[vars]
SITE_URL = "https://your-domain.pages.dev"
